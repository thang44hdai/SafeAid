<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ProfileOptionItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:background">@drawable/bg_option</item>
    </style>
    <style name="Theme.SafeAid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- giữ cho window vẽ system bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- status bar trong suốt -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- nếu muốn nav bar cũng trong suốt -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <style name="CircularImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>